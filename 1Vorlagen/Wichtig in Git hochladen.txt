1. Terminal öffnen mit (strg + ö)o.(Ansicht->Terminal)									Command Line
2. Änderungen Stagen mit																git add .
3. Änderungen committen mit			"Nachricht"->ändern									git commit -m "
4. Tag erstellen mit				"v0.0.3"/"Nachricht"->ändern						git tag -a v1.0.0 -m "
5. Änderungen pushen mit			"master"->Namen des Branches						git push origin master
6. Tag pushen mit					"v0.0.3"->muss = wie oben							git push origin v1.0.0

7. Terminal schließen


Fetig stellung, mit tutorial"