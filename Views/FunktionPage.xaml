<Page
    x:Class="App3.Views.FunktionPage"
    Style="{DynamicResource MahApps.Styles.Page}"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:properties="clr-namespace:App3.Properties"
    mc:Ignorable="d"
    d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="48" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock
            Style="{StaticResource PageTitleStyle}"
            Margin="{StaticResource MediumLeftMargin}"></TextBlock>
        <Grid
            Grid.Row="1"
            Margin="{StaticResource MediumLeftRightMargin}"
            Background="{DynamicResource MahApps.Brushes.Gray10}">
            <Button Content="Test"
                    HorizontalAlignment="Left"
                    Height="95" Margin="41,32,0,0"
                    VerticalAlignment="Top"
                    Width="180"
                    Click="Button_Click"/>
            <Button Content="Monitor Scan"
                    HorizontalAlignment="Left"
                    Height="75"
                    Margin="276,42,0,0"
                    VerticalAlignment="Top"
                    Width="115"
                    Click="ButMoniScann_Click"/>
            <Button Content="Initialisierung " HorizontalAlignment="Left" Height="160" Margin="541,22,0,0" VerticalAlignment="Top" Width="65" 
                    Click="IniziStart_Click"/>
            <!--
            The Mahapps Gray10 color represents where you should place your content.
            Place your content here.
            -->
        </Grid>
    </Grid>
</Page>
