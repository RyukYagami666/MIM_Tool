<controls:MetroWindow x:Class="MIM_Tool.Views.AuswahlFürISP"
                      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                      xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
                      Title="AuswahlFürISP" Height="450" Width="800"
                      Style="{StaticResource CustomMetroWindow}">
    <Grid>
        <Grid.ColumnDefinitions>

            <ColumnDefinition/>

            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Button x:Name="btnMonitorAuswahl1"
                Content="Button" 
                HorizontalAlignment="Center" 
                Height="100" Margin="0,160,0,0" 
                VerticalAlignment="Top" Width="270"
                Click="btnMonitorAuswahl1_Click"/>

        <Button x:Name="btnMonitorAuswahl2"
                Content="Button" 
                HorizontalAlignment="Center" 
                Height="100" Margin="0,160,0,0" 
                VerticalAlignment="Top" Width="270"
                Grid.Column="1"
                HorizontalContentAlignment="Center"
                Click="btnMonitorAuswahl2_Click"/>

        <Button x:Name="btnMonitorAuswahl3"
                Content="Button" 
                HorizontalAlignment="Center" 
                Height="100" Margin="0,290,0,0" 
                VerticalAlignment="Top" Width="270"
                HorizontalContentAlignment="Center"
                Click="btnMonitorAuswahl3_Click"/>

        <Button x:Name="btnMonitorAuswahl4"
                Content="Button" 
                HorizontalAlignment="Center" 
                Height="100" Margin="0,290,0,0" 
                VerticalAlignment="Top" Width="270"
                Grid.Column="1"
                HorizontalContentAlignment="Center"
                Click="btnMonitorAuswahl4_Click"/>


        <Label x:Name="lblMonitorAuswahl1" Content="Gewählte Icons zu einem Monitor zuweisen mit einem Klick auf gewünschten &#x0a;Monitor Button, wenn der Monitor schon zugewiesene Icons hat,  &#x0a;gibt es Folgende Möglichkeiten:  &#x0a;alle überschreiben; einzeln hinzufügen oder abbrechen " HorizontalAlignment="Center" Margin="0,0,0,304" Width="600" Grid.ColumnSpan="2" VerticalAlignment="Bottom" Height="100" BorderThickness="3" BorderBrush="Black" UseLayoutRounding="False" FontWeight="Bold" FontSize="14" HorizontalContentAlignment="Center" VerticalContentAlignment="Center">
            <Label.Style>
                <Style TargetType="Label">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Label">
                                <Border
                            Background="{DynamicResource MahApps.Brushes.Gray10}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="5">
                                    <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Label.Style>
        </Label>
    </Grid>
</controls:MetroWindow>
